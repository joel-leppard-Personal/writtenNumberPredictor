# writtenNumberPredictor
###hypothesis
given each number (0-9) as a probability function image, a number drawn by the user can be roughly but consistently correctly predicted by being compared to said probabilty function images
